# Examples

This folder holds generated demo artifacts.

Generate a sample RE map locally:

```bash
python -m detranspiler re-map --demo --out examples/re_map_demo.html
```

Open `examples/re_map_demo.html` in a browser to explore the interactive graph without running a full analysis.
